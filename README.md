# deployment-audit

`deployment-audit` is a deployment-audit engine for energy-aware AI systems.
It freezes benchmark semantics, executes policy families on versioned benchmark manifests,
constructs realized deployment menus, and exports audit artifacts that distinguish
contradiction visibility, policy exposure, deployment consequence, and frontier instability.